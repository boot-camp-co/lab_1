<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:camel="http://camel.apache.org/schema/spring"
	xmlns:context="http://www.springframework.org/schema/context"
	xsi:schemaLocation="
       http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
       http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd
       http://camel.apache.org/schema/spring http://camel.apache.org/schema/spring/camel-spring.xsd">
       
       <context:property-placeholder
        properties-ref="properties" />

	<bean id="helloBean" class="org.jboss.integration.amq.advisories.HelloBean">
		<property name="say" value="Hi from Camel" />
	</bean>

	<camelContext xmlns="http://camel.apache.org/schema/spring">
		<propertyPlaceholder id="properties"
			location="file:/home/brysmith/Fuse/jboss-fuse-6.1.0.redhat-379/etc/org.jboss.integration.amq.cfg" />
		<package>org.apache.camel.example.spring</package>

		<route>
			<from uri="timer:foo?period=500" />
			<setHeader headerName="JMSXGroupID">
				<simple>${properties:group}</simple>
			</setHeader>
			<setHeader headerName="JMSXGroupSeq">
				<method ref="helloBean" method="getSequence" />
			</setHeader>
			<setBody>
				<method ref="helloBean" method="hello" />
			</setBody>
			<log message="Messsage to Queue: ${body}" />
			<to uri="activemq:queue:org.jboss.integration.amq.queue.advisories.test" />
		</route>

		<route>
			<from
				uri="activemq2:queue:org.jboss.integration.amq.queue.advisories.test" />
			<log
				message="Messsage from Queue: ${in.headers.JMSXGroupID} ${in.headers.JMSXGroupSeq} - ${body}" />
		</route>
	</camelContext>

	<bean id="activemq" class="org.apache.activemq.camel.component.ActiveMQComponent">
		<property name="connectionFactory" ref="connectionFactory" />
	</bean>

	<bean id="connectionFactory" class="org.apache.activemq.ActiveMQConnectionFactory">
		<!-- property name="brokerURL" value="nio://localhost:61080?broker.persistent=true" / -->
		<property
            name="brokerURL"
            value="${producer.url}" />
		<property name="userName" value="admin" />
		<property name="password" value="admin" />
	</bean>
	
		<bean id="activemq2" class="org.apache.activemq.camel.component.ActiveMQComponent">
		<property name="connectionFactory" ref="connectionFactory2" />
	</bean>

	<bean id="connectionFactory2" class="org.apache.activemq.ActiveMQConnectionFactory">
		 <!-- 61616,61617,61618 -->
		<property
            name="brokerURL"
            value="${receiver.url}" />
		<property name="userName" value="admin" />
		<property name="password" value="admin" />
	</bean>

</beans>